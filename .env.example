DHAN_CLIENT_ID=
DHAN_PIN=
DHAN_TOTP_SECRET=
DHAN_ACCESS_TOKEN=

# Set to 1 if doctor.py reports IPv6 as the bottleneck. Forces IPv4-only DNS
# so requests do not stall on a dead IPv6 route before falling back.
DHAN_FORCE_IPV4=
